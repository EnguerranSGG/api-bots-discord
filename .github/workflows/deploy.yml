name: ProductionDeployer pipeline

on: 
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      
      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Copy files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.RSA_VPS_PRIVATE_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "./dist,./docker-compose.yml,./Dockerfile.prod,./package.json,./package-lock.json,./tsconfig.json,./tsconfig.build.json,./nest-cli.json,./src"
          target: "./api-bots-discord"
          strip_components: 0

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.RSA_VPS_PRIVATE_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: | 
            cd "./api-bots-discord"
            sudo docker-compose down
            sudo docker-compose pull
            sudo docker-compose --profile prod up --build -d
            sudo docker image prune -f
            sudo docker ps
